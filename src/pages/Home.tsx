import FlexContainer from "styles/layout/FlexContainer";
import ThemeButton from "components/ui/buttons/ThemeButton";

const Home = () => {
  return (
    <FlexContainer>
      <ThemeButton />
    </FlexContainer>
  );
};

export default Home;
